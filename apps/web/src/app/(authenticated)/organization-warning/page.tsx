export default function OrganizationWarning(): React.ReactNode {
  // eslint-disable-next-line react/jsx-no-useless-fragment -- we need to have a page.tsx, otherwise next throws an error
  return <></>;
}
