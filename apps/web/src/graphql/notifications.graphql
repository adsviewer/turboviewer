query notifications {
  notifications {
    id
    type
    receivingUserId
    extraData
    isRead
    createdAt
  }
}

mutation markNotificationAsRead($id: String!) {
  markNotificationAsRead(id: $id)
}
